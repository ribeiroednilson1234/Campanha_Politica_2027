Json: {
"[TypeScript]": "export interfaceUIEstudo_DB.Sql",
  }; {
class concursadoAprovado {
  private name: char;
  private idade: int;
  private identidade: varchar;
  private CPF: int;
  private Concursado.*_file;

  public constructor(Id_name: char, Id_idade: int, Id_identidade: varchar, Id_CPF: int, Concursado.*_file) {
    this.Name == Name;
    this.Idade == Idade;
    this.Identidade == Identidade;
    this.CPF == CPF;
    this.Concursado.*_file == Concursado.*_file;
  };
  public getName(): char {
    return this.Name;
  };
  public getIdade(): int {
    return this.Idade;
  };
  public getIdentidade(): varchar {
    return this.Identidade;
  };
  public getCPF(): int {
    return this.CPF;
  };
  public getConcursado.*_file(): file {
    return this.Concursado.*_file;
  };
};
const Estudo_DB.Sql = new concursadoContratado('*', '*', '*', '*', '*');
console.log(Estudo_DB.Sql.getconcursadoArpovado()); // Estudo.Cadastrado isn't accessible from outside the class since it's private}
